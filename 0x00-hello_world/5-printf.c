#include <stdio.h>
/**
 * main - Show a simple string using printf
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char varA[] = "with proper grammar, but the outcome is a piece of art,";
printf("%s\n", varA);
return (0);
}
